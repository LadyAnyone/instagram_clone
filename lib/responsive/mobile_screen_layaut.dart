import 'package:flutter/material.dart';

class MobileScreenLayaut extends StatelessWidget {
  const MobileScreenLayaut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is mobile'),
      ),
    );
  }
}
