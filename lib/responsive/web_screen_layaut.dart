import 'package:flutter/material.dart';

class WebScreenLayaut extends StatelessWidget {
  const WebScreenLayaut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is web'),
      ),
    );
  }
}
